# No third-party deps — all scripts use the Python standard library
# (urllib, json, csv, datetime, pathlib). Listed here so CI has a file to
# install from and contributors know the constraint is intentional.
